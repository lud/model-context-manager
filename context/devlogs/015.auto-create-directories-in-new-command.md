# Auto-create directories in new command

When running `mcm new`, the doctype's directory is now created automatically (with `mkdirSync recursive`) if it doesn't already exist. Previously the command would abort with an error if the directory was missing. Updated the test accordingly.
